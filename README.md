# Posts API

- To run the server 
    ```bash
        make
    ```

    Now hit http://localhost:6969

- To generate updated docs
    ```bash
        make docs
    ```

- To clean dependencies
    ```bash
        make clean
    ```